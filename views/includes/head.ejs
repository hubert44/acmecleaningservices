<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0">
    <title><%= title %></title>
    <meta name="description" content="Acme is a professional cleaning service company providing cleaning services for both residential & commercial, We offer a range of cleaning services across Lagos state Nigeria.">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.14.0/css/all.min.css" rel="stylesheet">
    <link href="/styles/bootcss.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick-theme.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/slick-carousel/1.8.1/slick.css" rel="stylesheet">
    <link href="/images/logo2.ico" rel="icon">
    <link href="/styles/loader.css" rel="stylesheet">
    <link href="/styles/styles.css" rel="stylesheet">
    <% if(style){ %>
    <link href="/<%= style %>" rel="stylesheet">
    <% }%>   
</head>
<body class="stopscroll">
    <div class="loader-wrap">
        <div class="loader">
            <span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span><span class="loader-item"></span>
        </div>
    </div>
  
    <nav class="nav">
        <div class="container">
            <div class="logo">
                <a href="/"><img src="/images/acme.png" alt="logo" height="20"></a>
            </div>
            <div id="mainListDiv" class="main_list">
                <ul class="navlinks">
                    <li><a href="/" class="<%= title === 'ACME - Home' ? 'active' : '' %>">Home</a></li>
                    <li><a href="/services" class="<%= title === 'ACME - Services' ? 'active' : '' %>">Services</a></li>
                    <li><a href="/book" class="<%= title === 'ACME - Booking' ? 'active' : '' %>">Book</a></li>
                    <li><a href="<%= title === 'ACME - Account' ? '/logout' : '/login' %>" class="<%= title === 'ACME - Signin' ? 'active' : '' %> <%= title === 'ACME - Account' ? 'logout' : '' %>">
                        <% if(loggedIn){ %><%= title === 'ACME - Account' ? 'Logout' : 'Account' %><% }else { %><%= 'Login' %><% } %>
                    </a></li>
                    <li><a href="/contact" class="<%= title === 'ACME - Contact' ? 'active' : '' %>">Contact</a></li>
                </ul>
            </div>
            <span class="navTrigger">
                <i></i>
                <i></i>
                <i></i>
            </span>
        </div>
    </nav>